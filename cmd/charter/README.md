# Charter CLI

Status: Planned; no command has been implemented.

The future `charter` command will:

- Validate instance documents against a selected schema version
- Run applicable conformance rules and fixtures
- Produce machine-readable and human-readable reports
- Identify requirement IDs for every validation failure
- Print its supported Charter specification and conformance versions

The CLI will be a reference tool, not the exclusive means of establishing conformance.
