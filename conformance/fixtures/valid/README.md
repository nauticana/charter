# Valid Fixtures

Each directory holds a `fixture.yaml` naming the rules it must pass and either instance documents or a `documents` path to a shared set. `harbor/` points at the Harbor instance documents, which pass every active rule. Scenario fixtures for runtime-behavioral rules also point at a document set and describe invocations a conforming runtime must carry out.
