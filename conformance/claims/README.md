# Reference Conformance Claims

Claims of the Go reference SDK against the active profiles of the release named by `conformance/manifest.yaml`, generated with `charter claim` and regenerated on every release; a test fails when they fall behind the manifest. They state what the reference implementation demonstrates and are not claims about any other implementation.
