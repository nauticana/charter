# Validation Package

`Validator` interface implemented by the structural validator (embedded schemas) and by every semantic `Rule` (concrete rule types embedding `AbstractRule`); `Runner` executes manifest fixtures. `ConformanceClaim` generation is planned. Results carry stable rule ids, normative requirement ids, verification classes, and document paths.
