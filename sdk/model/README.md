# Model Package

The future `model` package will contain public Go types corresponding to released Charter schemas. Generated files and hand-written behavior must be visibly separated. Generated types must not introduce fields or defaults absent from the normative contract.
