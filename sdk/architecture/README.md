# Architecture SDK

`Provider` resolves architecture states, gaps, and roadmap items (`BaseProvider` over any `corpus.Source`). `Checker` verifies gap and roadmap reference consistency and time-bounded states with decision authority, each issue linked to its `CHR-ARCH-*` requirement, and answers `StatesAt` effective-context queries. No architecture method is prescribed.
